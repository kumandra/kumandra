delegated-proof-of-replicate(pin)

I.
End User can become a validator by joinning IPFS node and participate in pinning CID hash.
Every validator start replicating the CID but only one validator get choosen to produce a new block.


II.
Farming option
TODO!
